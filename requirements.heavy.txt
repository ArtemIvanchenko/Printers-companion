# Heavy ML / SLM / 3D-geometry tier — installed ONLY in the api image, which runs
# STL slicing, print-time estimation, defect-risk ML and cross-session change-point
# detection inline in its route handlers. PythonSLM + pymeshfix compile C extensions
# (build-essential + cmake are installed and purged in Dockerfile.api).
# Requires requirements.analytics.txt (polars, scipy, networkx) to be installed first.
scikit-learn==1.4.2
lightgbm==4.6.0
trimesh==4.12.2
shapely==2.1.2
rtree==1.4.1
PythonSLM==0.6.1
pymeshfix==0.18.1
ruptures==1.1.10

# analytics/test_metrics — "Тестовые метрики" dashboard tab.
# Versions NOT hard-pinned (unlike the rest of this file) — need a real build
# to confirm no conflicts with the already-pinned scikit-learn/polars/scipy
# above, then pin whatever pip resolves.
pyod>=2.0
shap>=0.45
river>=0.21
pandas>=2.2
tsfresh>=0.20
