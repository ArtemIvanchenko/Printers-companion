.git
.pytest_cache
# Raw printer logs live here on the host (bind-mounted at runtime) — never bake
# them into the image via `COPY . /app`.
Logs/
**/__pycache__
**/*.py[cod]
.env
printer_logs.db
*.db
*.sqlite
*.sqlite3
dist
build
*.egg-info
.venv
venv
htmlcov
.coverage
